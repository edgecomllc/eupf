# open5gs-compose

This deployment starts Open5gs 4G mobile core with UPF.

## 1. Install docker, docker-compose

<https://docs.docker.com/engine/install/>

## 2. Start services

Run `docker-compose up -d`
