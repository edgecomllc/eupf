name: Test

on:
  push:
    branches:
      - "main"
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: awalsh128/cache-apt-pkgs-action@v1
        with:
          packages: clang llvm gcc-multilib libbpf-dev

      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '>=1.20.0'
          cache: true

      - name: Install swaggo
        run: go install github.com/swaggo/swag/cmd/swag@v1.8.12

      - name: Generate project dependencies
        run: go generate -v ./cmd/...

      - name: Go vet main
        run: go vet ./cmd/eupf

      - name: Go vet config
        run: go vet ./cmd/eupf/config

      - name: Go vet core
        run: go vet ./cmd/eupf/core

      - name: Go vet docs
        run: go vet ./cmd/eupf/docs

      - name: Go vet ebpf
        run: go vet ./cmd/eupf/ebpf

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v3
        with:
          working-directory: ./cmd/eupf

      - name: Test core
        run: sudo go test -v go test ./cmd/eupf/core

      - name: Test ebpf
        run: sudo go test -v go test ./cmd/eupf/ebpf
      
      - name: Build
        run: go build -v -o bin/eupf ./cmd/eupf
